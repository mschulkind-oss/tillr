# Binaries
bin/
/tillr

# Environment
.env
.venv/

# IDE
.idea/
.vscode/

# OS
.DS_Store
Thumbs.db

# Build artifacts
coverage.out
coverage.html

# Project data (gitignored — each user has their own)
context/

# Lifecycle databases (created at runtime)
*.db
*.db-wal
*.db-shm

# Node modules (for web dev tooling if needed)
node_modules/

# Lifecycle project config (created by `tillr init`)
.tillr.json
.pnpm-store/
